{
  "name": "PR detail Files changed tab renders the seeded diff",
  "description": "From PR #482, open the Files changed tab and confirm the unified-diff viewer renders a seeded file path (src/config.ts). Exercises the refactored diff-viewer (DiffViewer → FileCard → CodeLine). Read-only seeded data (no model call). Assumes a freshly-seeded DB (see e2e/README.md). Shares its 'navigate to PR #482' prefix with 02/04 via specs/fixtures/gotoPr482.json.",
  "steps": [
    { "use": "gotoPr482" },
    { "cmd": ["find", "role", "button", "click", "--name", "Files changed"], "label": "switch to the Files changed tab" },
    { "cmd": ["wait", "--url", "tab=diff"], "label": "diff tab is active in the URL" },
    { "cmd": ["wait", "--text", "src/config.ts"], "label": "a seeded changed file renders in the diff viewer" }
  ]
}
