# e2e — Specs

Feature, API, and schema specs — the contract a change in `e2e/` must satisfy
(e.g. what a new flow spec must cover). One file per feature/contract, named
`<feature>.md`. When a spec changes after shipping, add a new dated section
rather than silently rewriting the original decision.
