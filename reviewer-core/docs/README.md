# reviewer-core — Docs

Architecture deep-dives and file-by-file/module-by-module documentation for
`reviewer-core/`. Not loaded into every agent session — `AGENTS.md` links here
only when a task needs this depth.

<!-- Add one file per subsystem/topic as this fills in. -->
