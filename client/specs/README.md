# client — Specs

Feature, API, and schema specs — the contract a change in `client/` must
satisfy. One file per feature/contract, named `<feature>.md`. When a spec
changes after shipping, add a new dated section rather than silently rewriting
the original decision.
